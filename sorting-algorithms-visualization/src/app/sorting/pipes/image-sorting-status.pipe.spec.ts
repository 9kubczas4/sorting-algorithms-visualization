import { ImageSortingStatusPipe } from './image-sorting-status.pipe';

describe('ImageSortingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSortingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
